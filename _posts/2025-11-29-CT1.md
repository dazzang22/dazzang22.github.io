---



---

* 정적 배열의 초기화는 필수

```c++
int a[10] = {};
```

* 배열의 사이즈 구하는 법

```c++
int a[10];
int size = sizeof(a)/sizeof(a[0]);
```

* 숫자 각 자리 검사 방법 2가지 

  * String 변환

  ```c++
  int a = 100000;
  string s = to_string(a);
  for (char ch : s){
    . 
    .
  }
  ```

  * 10% 연산

  ```c++
  int num = 12345;
  int a = num / 100000;
  num %= 100000;
  int b = num / 1000;
  	.
    .
    .
  ```

  

* char 를 string 으로 캐스팅하는법 -> 직접 캐스팅이 안됨

```c++
for (char c : s)
  string tmp(1,c)
```



* int 를 문자열로 변환하는 법

```c++
to_string
```

